- defaults:
    link:
      relink: true

- clean: ['~']

- link:
#    ~/.tmux.conf: tmux.conf
#    ~/.vim: vim
    ~/.zshrc: zshrc

- create:
    - ~/downloads
    - ~/PycharmProjects
    - ~/RustroverProjects
    - ~/IdeaProjects
#    - ~/.vim/undo-history

- shell:
  - [git submodule update --init --recursive, Installing submodules]
