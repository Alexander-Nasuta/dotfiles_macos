#!/usr/bin/env zsh

echo "\n <<<Starting Homebrew Setup >>> \n"


# install homebrew as shown on its website (https://brew.sh)
# /bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"


# install via brewfile
brew bundle --verbose
